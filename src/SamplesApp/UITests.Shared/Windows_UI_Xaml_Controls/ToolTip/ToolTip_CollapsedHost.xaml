<UserControl x:Class="UITests.Windows_UI_Xaml_Controls.ToolTip.ToolTip_CollapsedHost"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:ex="using:UITests.Shared.Windows_UI_Xaml.MarkupExtension"
			 xmlns:local="using:UITests.Windows_UI_Xaml_Controls.ToolTip">

	<StackPanel Margin="0,100,0,0">
		<ToggleButton x:Name="MainToggle"
					  IsChecked="True"
					  Content="Toggle without Tooltip" />

		<TextBlock Text="Click the button, and quickly move the mouse away" />
		<ToggleButton x:Name="ToggleHide"
					  Content="Hide"
					  IsChecked="{Binding ElementName=MainToggle, Path=IsChecked, Mode=TwoWay}"
					  Visibility="{Binding ElementName=MainToggle, Path=IsChecked}">
			<ToolTipService.ToolTip>
				<TextBlock x:Name="TooltipText" Text="Tooltip associated with 'Hide' button" />
			</ToolTipService.ToolTip>
		</ToggleButton>
	</StackPanel>

</UserControl>
